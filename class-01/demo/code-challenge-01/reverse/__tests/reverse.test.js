'use strict';

let reverseArray = require('../reverse.js');

// reverseArray is a function
// reverseArray is the function exported from reverse.js

let myNumbers = [2, 4, 6, 8, 10];
reverseArray(myNumbers);

console.log(myNumbers);